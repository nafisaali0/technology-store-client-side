// import PropTypes from 'prop-types';

const AuthProvider = () => {
    return (
        <div>
            
        </div>
    );
};

// AuthProvider.propTypes = {
    
// };

export default AuthProvider;